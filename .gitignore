.venv/
__pycache__/
*.pyc
output/
output_*/
*.log
.DS_Store
.env

# editor / agent config
.kiro/
.agents/
.claude/
.codex/
.muse/
.venv/
.pytest_cache/
.ruff_cache/
attic/
conversation-*.txt
input_assets/character-poc/
input_assets/sprint2-ingredients/
static/
tests/browser/
cdk.out/
test-results/
output_kodiak*/

# node (only infra-cdk uses node; its own .gitignore handles cdk.out/compiled)
node_modules/
package-lock.json
/package-lock.json

# generated docs + local doc-gen tooling (not part of the shipped pipeline)
README.html
docs/kodiak/
scripts/gen-readme-html.py
scripts/vendor/

# terraform
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
data/raw-ingest/*
!data/raw-ingest/kodiakcakes/*.json
!data/raw-ingest/kodiakcakes/brand-lore/


# rust build artifacts (regenerable, ~115M)
rust/**/target/
*.rlib
*.so
*.pyd

# large binaries hosted in S3 DAM (chasko-creative-dam-946179428633-us-east-1), not git
input_assets/power-cakes/hero-real.png
web/kodiak-posts-for-todays-frontier/input_assets/*
# exception: campaign-web derivatives ship with the offline page (built by
# scripts/build-campaign-web-art.py, ~70KB each) — the resting hero swaps to
# them with zero fetch, so they must exist on fresh checkouts.
!web/kodiak-posts-for-todays-frontier/input_assets/campaign-web/


# derived data hosted in S3 (brands/kodiak/vectors/), regenerable, not git
data/vectors/kodiak-embeddings.jsonl

# 644 downloaded hero images, re-downloadable, belong in S3 not git
data/raw-ingest/kodiakcakes/blog-images/

tests/live/videos/

# deploy-time mirror of DAM recipe-art (scripts/deploy-frontier.sh syncs it
# into the site tree; objects are content, not source)
web/kodiak-posts-for-todays-frontier/recipe-art/
