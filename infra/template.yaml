AWSTemplateFormatVersion: "2010-09-09"
Description: Kodiak creative pipeline - main stack (kodiak-creatives). Style library DAM bucket (imported), localization memory, retail network, log bucket, and CI. Retains buckets/tables on delete.
Parameters:
  ProjectName:
    Type: String
    Default: kodiak-creatives
  BucketName:
    Type: String
    Default: chasko-creative-dam-946179428633-us-east-1
    Description: Cloud storage bucket name - must be globally unique. Current live bucket is chasko-creative-dam-946179428633-us-east-1 in us-east-1. Change only if you need a second environment.

Resources:
  # NOTE: Observability resources (log group, X-Ray sampling rule, write policy) live in
  # infra/observability.yaml / stack kodiak-creatives-observability - deliberately separate
  # to avoid the DAM-bucket import collision. Do not re-add them here.
  #
  # Tagging strategy (applied to every taggable resource in this template):
  #   project=kodiak-creatives (via !Ref ProjectName), team=platform,
  #   managed-by=cloudformation, repo=chasko-labs/creative-automation-pipeline,
  #   environment=production, stack=kodiak-creatives.
  # S3 / DynamoDB / IAM Role / CodeBuild all model Tags as a LIST of {Key,Value}.
  # S3::BucketPolicy has no Tags property (omitted below).

  # Cloud storage for style and photos - versioned, private, encrypted.
  # IMPORTED resource: properties below MUST match the live bucket or import fails.
  # Live config verified us-east-1 acct 946179428633: versioning Enabled,
  # aws:kms + BucketKeyEnabled true, DenyInsecureTransport policy present.
  StyleLibraryBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !Ref BucketName
      VersioningConfiguration:
        Status: Enabled
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: aws:kms
            BucketKeyEnabled: true
      LifecycleConfiguration:
        Rules:
          # Filter Prefix "" matches the live rule shape (applies bucket-wide).
          # CloudFormation models the S3 filter prefix as the rule-level Prefix property.
          - Id: abort-multipart
            Status: Enabled
            Prefix: ""
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 7
          # Scoped to brands/kodiak/renders/ - matches live; tiering applies to renders only
          - Id: renders-archive
            Status: Enabled
            Prefix: brands/kodiak/renders/
            Transitions:
              - StorageClass: STANDARD_IA
                TransitionInDays: 90
              - StorageClass: GLACIER
                TransitionInDays: 180
              - StorageClass: DEEP_ARCHIVE
                TransitionInDays: 365
            NoncurrentVersionTransitions:
              - StorageClass: STANDARD_IA
                TransitionInDays: 30
                NewerNoncurrentVersions: 2
      Tags:
        - Key: project
          Value: !Ref ProjectName
        - Key: brand
          Value: kodiak
        - Key: team
          Value: platform
        - Key: managed-by
          Value: cloudformation
        - Key: repo
          Value: chasko-labs/creative-automation-pipeline
        - Key: environment
          Value: production
        - Key: stack
          Value: kodiak-creatives

  # DeletionPolicy Retain so the bucket policy that already exists on the live bucket
  # can be imported alongside the bucket. S3::BucketPolicy has no Tags property.
  StyleLibraryBucketPolicyTLSOnly:
    Type: AWS::S3::BucketPolicy
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      Bucket: !Ref StyleLibraryBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: DenyInsecureTransport
            Effect: Deny
            Principal: "*"
            Action: s3:*
            Resource:
              - !Sub ${StyleLibraryBucket.Arn}
              - !Sub ${StyleLibraryBucket.Arn}/*
            Condition:
              Bool:
                aws:SecureTransport: false

  # Table that remembers what worked per place - one row per creative per place
  LocalizationMemoryTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub ${ProjectName}-localization-memory
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: market
          AttributeType: S
        - AttributeName: place_message_id
          AttributeType: S
      KeySchema:
        - AttributeName: market
          KeyType: HASH
        - AttributeName: place_message_id
          KeyType: RANGE
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      Tags:
        - Key: project
          Value: !Ref ProjectName
        - Key: team
          Value: platform
        - Key: managed-by
          Value: cloudformation
        - Key: repo
          Value: chasko-labs/creative-automation-pipeline
        - Key: environment
          Value: production
        - Key: stack
          Value: kodiak-creatives

  # Retail network - every store that carries Kodiak, one row per storefront
  RetailNetworkTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub ${ProjectName}-retail-network
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: store_id
          AttributeType: S
        - AttributeName: market
          AttributeType: S
      KeySchema:
        - AttributeName: store_id
          KeyType: HASH
      GlobalSecondaryIndexes:
        - IndexName: byMarket
          KeySchema:
            - AttributeName: market
              KeyType: HASH
          Projection:
            ProjectionType: ALL
      Tags:
        - Key: project
          Value: !Ref ProjectName
        - Key: team
          Value: platform
        - Key: managed-by
          Value: cloudformation
        - Key: repo
          Value: chasko-labs/creative-automation-pipeline
        - Key: environment
          Value: production
        - Key: stack
          Value: kodiak-creatives

  # Searchable knowledge - place, audience, message, photo cue - via vector storage
  # Using S3 Vectors (managed, no servers) with S3VectorsConfiguration for the knowledge base
  # See docs at https://docs.aws.amazon.com/bedrock/latest/userguide/kb-permissions.html for full policy

  CreativePipelineLogBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !Sub ${ProjectName}-logs-${AWS::AccountId}-${AWS::Region}
      VersioningConfiguration:
        Status: Enabled
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: aws:kms
            BucketKeyEnabled: true
      Tags:
        - Key: project
          Value: !Ref ProjectName
        - Key: team
          Value: platform
        - Key: managed-by
          Value: cloudformation
        - Key: repo
          Value: chasko-labs/creative-automation-pipeline
        - Key: environment
          Value: production
        - Key: stack
          Value: kodiak-creatives

  # CI service role - least privilege. The per-push gate is ruff + pytest + cfn-lint,
  # none of which make aws api calls (cfn-lint runs offline). So the only grant this
  # role needs is CloudWatch Logs to its own log group. No S3 / DynamoDB / Bedrock / SSM.
  # Explicit RoleName (${ProjectName}-ci-role) means the deploy needs CAPABILITY_NAMED_IAM.
  CodeBuildServiceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${ProjectName}-ci-role
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: codebuild.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: cloudwatch-logs
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: WriteOwnLogGroup
                Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource:
                  - !Sub arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/codebuild/${ProjectName}-ci
                  - !Sub arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/codebuild/${ProjectName}-ci:*
      Tags:
        - Key: project
          Value: !Ref ProjectName
        - Key: team
          Value: platform
        - Key: managed-by
          Value: cloudformation
        - Key: repo
          Value: chasko-labs/creative-automation-pipeline
        - Key: environment
          Value: production
        - Key: stack
          Value: kodiak-creatives

  # Fast per-push CI gate. Webhook fires on push and on PR create/update/reopen.
  # RUN_SLOW pinned false here so the slow path (render/nova-act/embedding) never
  # runs on the per-push gate - it belongs to nightly/manual runs only.
  # GITHUB source + Webhook requires an account-level source credential imported first.
  CreativePipelineCI:
    Type: AWS::CodeBuild::Project
    Properties:
      Name: !Sub ${ProjectName}-ci
      Description: !Sub Fast per-push CI gate for ${ProjectName} - ruff, pytest, cfn-lint
      ServiceRole: !GetAtt CodeBuildServiceRole.Arn
      TimeoutInMinutes: 20
      Source:
        Type: GITHUB
        Location: https://github.com/chasko-labs/creative-automation-pipeline.git
        BuildSpec: buildspec.yml
        ReportBuildStatus: true
        GitCloneDepth: 1
      Artifacts:
        Type: NO_ARTIFACTS
      Environment:
        Type: LINUX_CONTAINER
        ComputeType: BUILD_GENERAL1_SMALL
        Image: aws/codebuild/amazonlinux2-x86_64-standard:5.0
        EnvironmentVariables:
          - Name: RUN_SLOW
            Value: "false"
            Type: PLAINTEXT
      Triggers:
        Webhook: true
        FilterGroups:
          - - Type: EVENT
              Pattern: PUSH
          - - Type: EVENT
              Pattern: PULL_REQUEST_CREATED,PULL_REQUEST_UPDATED,PULL_REQUEST_REOPENED
      LogsConfig:
        CloudWatchLogs:
          Status: ENABLED
          GroupName: !Sub /codebuild/${ProjectName}-ci
      Tags:
        - Key: project
          Value: !Ref ProjectName
        - Key: team
          Value: platform
        - Key: managed-by
          Value: cloudformation
        - Key: repo
          Value: chasko-labs/creative-automation-pipeline
        - Key: environment
          Value: production
        - Key: stack
          Value: kodiak-creatives

Outputs:
  StyleLibraryBucketName:
    Value: !Ref StyleLibraryBucket
    Description: Cloud storage bucket - use as DAM_S3_BUCKET with prefix brands/kodiak/
  StyleLibraryPrefix:
    Value: brands/kodiak/
  LocalizationMemoryTableName:
    Value: !Ref LocalizationMemoryTable
  RetailNetworkTableName:
    Value: !Ref RetailNetworkTable
  LogBucketName:
    Value: !Ref CreativePipelineLogBucket
  CodeBuildProjectName:
    Value: !Ref CreativePipelineCI
    Description: Per-push CI gate project name
  CodeBuildServiceRoleArn:
    Value: !GetAtt CodeBuildServiceRole.Arn
    Description: Least-privilege CodeBuild service role arn (CloudWatch Logs only)
  NextSteps:
    Value: !Sub "export DAM_S3_BUCKET=${StyleLibraryBucket} DAM_S3_PREFIX=brands/kodiak/ && ./scripts/sync-dam.sh push"
